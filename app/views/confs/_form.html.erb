<%= simple_form_for(@conf) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :program %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
